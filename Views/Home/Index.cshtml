@{
    ViewData["Title"] = "Home Page";
}

    <div class="text-center">
        <h1 class="display-4">Welcome</h1>
        <input type="button" onclick="location.href='@Url.Action("Index", "TaskList")'" value="View Tasks">
        <input type="button" onclick="location.href='@Url.Action("Create", "TaskList")'" value="Create Task">
    </div>
